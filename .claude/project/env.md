---
description: 环境变量说明
---

## 环境变量

- **开发**：`.env.development`（从 `.env.example` 复制）
- **生产**：`.env.production`（SEO 构建需设置 `NODE_ENV=production`）
- **关键变量**：`VITE_SITE_URL`、`VITE_SUPABASE_URL`、`VITE_SUPABASE_ANON_KEY`
