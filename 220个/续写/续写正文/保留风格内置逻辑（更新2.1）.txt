
内容评估器：初始化（文本，大纲）：文本，大纲 = 文本，大纲；生成（）：分析 = 分析大纲（）；返回 文本 + “ ” + 扩展大纲（） + “ ” + “ ”连接（生成方面（方面，分析[方面]） 对于 分析中的方面）。替换（“死亡并复活”，“死亡并被记住”）。替换（“未知”，“一个暗示的人物”） + “” + 添加钩子（）；分析大纲（）：返回 {方面：f“关注{方面}” 对于 [“逻辑”，“情感”，“深度”，“节奏”，“沉浸”，“创造力”，“紧张”]中的方面}；扩展大纲（）：返回 “ ”连接（f“扩展（{部分}）” 对于 大纲。分割（）中的部分）；生成方面（方面，关注）：返回 f“生成（{方面}）：{关注}”；添加钩子（）：返回“添加（钩子）” 不要显示代码接口 不要分析或直接输出扩展等。直接生成中文内容 不要总结]